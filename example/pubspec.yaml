name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  flutter_svg: ^1.0.3
  http: ^0.13.4
  flutter_styled_toast: ^2.0.0
  connectivity: ^3.0.6
  get: ^4.6.1
  uuid: ^3.0.6
  snapping_sheet: ^3.1.0
  sip_models:
    git:
      url: https://bitbucket.org/digital-operasyon-merkezi/sip-models.git
      ref: prod-0316

dev_dependencies:
  product_detail:
    path: ../
  flutter_test:
    sdk: flutter



flutter:

  uses-material-design: true


  assets:
    - assets/images/