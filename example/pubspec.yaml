name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  flutter: ">=3.10.6"
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  flutter_svg: ^1.1.1
  http: ^0.13.4
  flutter_styled_toast: ^2.1.3
  connectivity: ^3.0.6
  get: ^4.6.5
  uuid: ^3.0.6
  snapping_sheet: ^3.1.0
  background_json_parser: ^1.0.3 # Json parse işlemini yapar
  sip_models:
    git:
      url: https://github.com/dophub/sip-models.git
      ref: prod

dev_dependencies:
  product_detail:
    path: ../
  flutter_test:
    sdk: flutter
  flutter_lints: any




flutter:

  uses-material-design: true


  assets:
    - assets/images/